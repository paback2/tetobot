"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "Bag", {
    enumerable: true,
    get: function() {
        return Bag;
    }
});
const _utils = require("../../utils");
class Bag {
    rng;
    id = 0;
    extra = [];
    lastGenerated = null;
    constructor(seed){
        this.rng = new _utils.RNG(seed);
    }
    snapshot() {
        return {
            rng: this.rng.seed,
            id: this.id,
            extra: this.extra.slice(),
            lastGenerated: this.lastGenerated
        };
    }
    // note: not static because of inheritance
    fromSnapshot(snapshot) {
        this.rng = new _utils.RNG(snapshot.rng);
        this.id = snapshot.id;
        this.extra = snapshot.extra.slice();
        this.lastGenerated = snapshot.lastGenerated;
    }
}

//# sourceMappingURL=core.js.map