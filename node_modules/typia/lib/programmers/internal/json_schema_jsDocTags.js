"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.json_schema_jsDocTags = void 0;
const json_schema_jsDocTags = (schema, jsDocTags) => {
    var _a;
    for (const tag of jsDocTags) {
        if (tag.name.startsWith("x-") === false)
            continue;
        const value = (_a = tag.text) === null || _a === void 0 ? void 0 : _a.filter((s) => s.kind === "text").map((s) => s.text.trim().split("\r\n").join("\n"))[0];
        if (value === undefined)
            continue;
        else
            schema[tag.name] = cast(value);
    }
    return schema;
};
exports.json_schema_jsDocTags = json_schema_jsDocTags;
const cast = (value) => {
    if (value === "true")
        return true;
    if (value === "false")
        return false;
    const num = Number(value);
    if (Number.isNaN(num) === false)
        return num;
    return value === "null" ? null : value;
};
//# sourceMappingURL=json_schema_jsDocTags.js.map