import { Room } from ".";
import { Events, type Game, type VersusReplay } from "../../types";
import { Game as GameClass } from "../game";
export declare class ReplayManager {
    #private;
    constructor(readyPlayers: Game.Ready["players"], roomPlayers: Room["players"]);
    addRound(players: Game.Ready["players"]): void;
    pipe({ gameid, frames }: {
        gameid: number;
        frames: Game.Replay.Frame[];
    }): void;
    die({ gameid, data, game }: {
        gameid: number;
        data?: Events.in.Game["game.replay.end"]["data"];
        game?: GameClass;
    }): void;
    endRound({ game }: {
        game?: GameClass;
    }): void;
    end({ self }: {
        self: string;
    }): void;
    export(): VersusReplay;
}
//# sourceMappingURL=replayManager.d.ts.map