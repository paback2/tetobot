import { Mino } from "../queue";
export interface BoardInitializeParams {
    width: number;
    height: number;
    buffer: number;
}
export type Tile = {
    mino: Mino;
    connections: number;
} | null;
export declare enum BoardConnections {
    TOP = 8,
    RIGHT = 4,
    BOTTOM = 2,
    LEFT = 1,
    CORNER = 16,
    ALL = 15
}
export declare class Board {
    #private;
    state: Tile[][];
    constructor(options: BoardInitializeParams);
    get height(): number;
    set height(value: number);
    get width(): number;
    set width(value: number);
    get buffer(): number;
    set buffer(value: number);
    get fullHeight(): number;
    occupied(x: number, y: number): boolean;
    add(...blocks: [Tile, number, number][]): void;
    clearLines(): {
        lines: number;
        garbageCleared: number;
    };
    clearBombs(placedBlocks: [number, number][]): {
        lines: number;
        garbageCleared: number;
    };
    clearBombsAndLines(placedBlocks: [number, number][]): {
        lines: number;
        garbageCleared: number;
    };
    get perfectClear(): boolean;
    insertGarbage({ amount, size, column, bombs, isBeginning, isEnd }: {
        amount: number;
        size: number;
        column: number;
        bombs: boolean;
        isBeginning: boolean;
        isEnd: boolean;
    }): void;
}
export * from ".";
//# sourceMappingURL=index.d.ts.map