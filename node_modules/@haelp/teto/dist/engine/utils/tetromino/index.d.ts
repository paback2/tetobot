import type { Tile } from "../..";
import { Mino } from "../../queue/types";
import { type KickTable } from "../kicks";
import type { Rotation } from "./types";
export interface TetrominoInitializeParams {
    symbol: Mino;
    initialRotation: Rotation;
    boardHeight: number;
    boardWidth: number;
    from?: Tetromino | TetrominoSnapshot;
}
export interface TetrominoSnapshot {
    symbol: Mino;
    location: [number, number];
    locking: number;
    lockResets: number;
    rotResets: number;
    safeLock: number;
    highestY: number;
    rotation: Rotation;
    fallingRotations: number;
    totalRotations: number;
    irs: number;
    ihs: boolean;
    aox: number;
    aoy: number;
    keys: number;
}
export declare class Tetromino {
    #private;
    symbol: Mino;
    states: [number, number][][];
    location: [number, number];
    locking: number;
    lockResets: number;
    rotResets: number;
    safeLock: number;
    highestY: number;
    fallingRotations: number;
    totalRotations: number;
    irs: number;
    ihs: boolean;
    aox: number;
    aoy: number;
    keys: number;
    constructor(options: TetrominoInitializeParams);
    get blocks(): [number, number][];
    get absoluteBlocks(): [number, number][];
    absoluteAt({ x, y, rotation }: {
        x?: number;
        y?: number;
        rotation?: number;
    }): [number, number][];
    get rotation(): Rotation;
    set rotation(value: number);
    get x(): number;
    set x(value: number);
    get y(): number;
    set y(value: number);
    isStupidSpinPosition(board: Tile[][]): boolean;
    isAllSpinPosition(board: Tile[][]): boolean;
    rotate(board: Tile[][], kickTable: KickTable, amt: Rotation, maxMovement: boolean): boolean | {
        kick: [number, number];
        newLocation: [number, number];
        id: string;
        index: number;
    };
    moveRight(board: Tile[][]): boolean;
    moveLeft(board: Tile[][]): boolean;
    dasRight(board: Tile[][]): boolean;
    dasLeft(board: Tile[][]): boolean;
    softDrop(board: Tile[][]): boolean;
    snapshot(): TetrominoSnapshot;
}
export * from "./data";
export * from "./types";
//# sourceMappingURL=index.d.ts.map