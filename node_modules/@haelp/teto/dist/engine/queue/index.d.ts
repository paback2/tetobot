import { type BagType } from "./rng";
import type { Bag, BagSnapshot } from "./rng";
import { Mino } from "./types";
export interface QueueInitializeParams {
    seed: number;
    type: BagType;
    minLength: number;
}
export interface QueueSnapshot {
    value: Mino[];
    bag: BagSnapshot;
}
export declare class Queue extends Array<Mino> {
    #private;
    seed: number;
    type: BagType;
    bag: Bag;
    _minLength: number;
    repopulateListener: ((pieces: Mino[]) => void) | null;
    static get [Symbol.species](): ArrayConstructor;
    constructor(options: QueueInitializeParams);
    reset(): void;
    onRepopulate(listener: NonNullable<typeof this.repopulateListener>): void;
    get minLength(): number;
    set minLength(val: number);
    get next(): Mino;
    shift(): Mino | undefined;
    snapshot(): QueueSnapshot;
    fromSnapshot(snapshot: QueueSnapshot): void;
    raw(): Mino[];
}
export * from "./rng";
export * from "./types";
//# sourceMappingURL=index.d.ts.map